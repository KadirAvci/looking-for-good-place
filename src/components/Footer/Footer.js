import React from 'react';

const footer = (props) => {
    return (
        <footer className={"bg-dark text-white text-center p-2"}>
            @kadDev Tout droit réservé
        </footer>
    );
};

export default footer;